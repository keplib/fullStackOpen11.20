version: '3.8'
services:
  mongodb-test:
    image: mongo
    container_name: db-test
    environment:
      - MONGO_INITDB_DATABASE=phonebook_dev
    ports:
      - '27017:27017'
    # volumes:
    # - ./data:/data/db
    # - ./scripts:/docker-entrypoint-initdb.d
